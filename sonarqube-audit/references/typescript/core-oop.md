# core-oop
- typescript:S2430 — Constructor names should start with an upper case letter (MAJOR)
- typescript:S3525 — Class methods should be used instead of "prototype" assignments (MAJOR)
- typescript:S3854 — "super()" should be invoked appropriately (MAJOR)
- typescript:S4275 — Getters and setters should access the expected fields (MAJOR)
- typescript:S1534 — Member names should not be duplicated within a class or object literal (MAJOR)
- typescript:S2376 — Property getters and setters should come in pairs (MAJOR)
- typescript:S2933 — Fields that are only assigned in the constructor should be "readonly" (MAJOR)
- typescript:S1444 — Public "static" fields should be read-only (MAJOR)
- typescript:S6551 — Objects and classes converted or coerced to strings should define a "toString()" method (MAJOR)
