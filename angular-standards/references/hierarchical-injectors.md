# Hierarchical injectors

## Regras

- `providers` exposto a projected content.
- `viewProviders` somente para view local.

## Modificadores

- `optional`, `self`, `skipSelf`, `host` quando precisar de controle fino.
