[remote.github]
owner = "hebertpaziam"
repo = "skills"

[changelog]
header = """
# Changelog

Este arquivo e gerado automaticamente a partir do historico de Conventional Commits.
Nao edite manualmente.

"""
body = """
{% if version -%}
## {{ version | trim_start_matches(pat="v") }} - {{ timestamp | date(format="%Y-%m-%d") }}
{% else -%}
## Unreleased
{% endif -%}

{% for group, commits in commits | group_by(attribute="group") %}
### {{ group }}
{% for commit in commits %}
- {% if commit.scope %}**{{ commit.scope }}:** {% endif %}{% if commit.breaking %}[breaking] {% endif %}{{ commit.message | upper_first }}
{% endfor %}
{% endfor %}

"""
footer = """
<!-- generated by git-cliff -->
"""
trim = true

[git]
conventional_commits = true
filter_unconventional = true
protect_breaking_commits = true
filter_commits = true
tag_pattern = "v[0-9].*"
sort_commits = "newest"
commit_parsers = [
  { message = "^chore\\(changelog\\):", skip = true },
  { message = "^feat", group = "Novidades" },
  { message = "^fix", group = "Correcoes" },
  { message = "^docs?", group = "Documentacao" },
  { message = "^refactor", group = "Refatoracao" },
  { message = "^perf", group = "Performance" },
  { message = "^test", group = "Testes" },
  { message = "^build", group = "Build" },
  { message = "^ci", group = "CI" },
  { message = "^style", group = "Estilo" },
  { message = "^chore", group = "Manutencao" },
  { message = "^revert", group = "Reversoes" },
]
link_parsers = [
  { pattern = "#(\\d+)", href = "https://github.com/hebertpaziam/skills/issues/$1" },
]
